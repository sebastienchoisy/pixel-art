import React from 'react';

// Initialisation du context pour le darkmode de l'application
// eslint-disable-next-line import/prefer-default-export
export const ThemeContext = React.createContext('light');
